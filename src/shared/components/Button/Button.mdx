import { Meta, Story } from '@storybook/addon-docs';
import * as ButtonStories from './Button.stories';

<Meta title="Components/Button" of={ButtonStories} />

# Button

`Button` é um component de navegação.

## Exemplos
<Story of={ButtonStories.Default} />

<br/><br/>
### Descrição das Propriedades

<div className="flex flex-row flex-wrap" style={{ gap: "1rem" }}>
  <div>
    #### Propriedades do Button (Default atributes)

    <table>
      <thead>
        <tr>
          <th>Propriedade</th>
          <th>Tipo</th>
          <th>Descrição</th>
        </tr>
      </thead>
      <tbody>
        <tr>
          <td>children</td>
          <td>React.ReactNode</td>
          <td>Conteúdo do container.</td>
        </tr>
        <tr>
          <td>calssName</td>
          <td>string | string[] (Opcional)</td>
          <td>Passar estilos extras para o container.</td>
        </tr>
      </tbody>
    </table>
  </div>
</div>
